import { NextApiRequest, NextApiResponse } from 'next'

// import { graphQLClient, opentok, Session } from '~/pages/api/common'

// let query = /* GraphQL */ `
//     mutation UpdateSession($id: ID!) {
//         updateSession(id: $id }) {
//             id
//             _id
//         }
//     }
// `

// export const updateSession = async (id: string) => graphQLClient.request(query, { id })

// export default async (req: NextApiRequest, res: NextApiResponse) => {
//     try {
//         if (req.method !== 'POST') {
//             throw Error('Only POST allowed')
//         }
//         res.status(200).end('nothing')
//     } catch (error) {
//         console.error(error)
//         return res.status(error.status || 500).end(error.message)
//     }
// }
